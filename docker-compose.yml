services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: csm
      POSTGRES_PASSWORD: csm_dev_123
      POSTGRES_DB: cs_manager
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
